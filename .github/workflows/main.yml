name: CI
on: [push]
env:
  JAVA_PACKAGE_PATH: src/main/java/com
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install bazel
      uses: ngalaiko/bazel-action/1.2.1@master
    - run: |
        echo test again
    - name: Build weather
      run: |
        bazel build $JAVA_PACKAGE_PATH/weather:weather_deploy.jar
      shell: bash
